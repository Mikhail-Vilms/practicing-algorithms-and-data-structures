using System.Collections.Generic;

namespace PracticingAlgorithmsAndDataStructures.BinarySearchTree
{
    public class KthSmallestElement
    {
        //public static int Find(BSTNode root, int k)
        //{
        //    Stack<BSTNode> stack = new Stack<BSTNode>();

        //    while (true)
        //    {
        //        // traverse all left nodes to the final leaf
        //        while (root != null)
        //        {
        //            stack.Push(root);
        //            root = root.left;
        //        }

        //        // check current node; return if counter is 0
        //        k--;
        //        root = stack.Pop();
        //        if (k == 0)
        //        {
        //            return root.val;
        //        }

        //        // jump to the right node
        //        root = root.right;
        //    }
        //}
    }
}
